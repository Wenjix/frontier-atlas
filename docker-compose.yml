services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: frontier_atlas
      POSTGRES_USER: ubuntu
      POSTGRES_PASSWORD: frontier_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
